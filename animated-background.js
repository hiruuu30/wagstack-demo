import {
  S as Scene,
  O as OrthographicCamera,
  W as WebGLRenderer,
  P as PlaneGeometry,
  a as ShaderMaterial,
  V as Vector2,
  M as Mesh
} from './three-yEJwMVQ6.js';
const simplexNoise=`vec3 mod289(vec3 x){return x-floor(x*(1./289.))*289.;}vec4 mod289(vec4 x){return x-floor(x*(1./289.))*289.;}vec4 permute(vec4 x){return mod289(((x*34.)+1.)*x);}vec4 taylorInvSqrt(vec4 r){return 1.79284291400159-.85373472095314*r;}float snoise(vec3 v){const vec2 C=vec2(1./6.,1./3.);const vec4 D=vec4(0.,.5,1.,2.);vec3 i=floor(v+dot(v,C.yyy));vec3 x0=v-i+dot(i,C.xxx);vec3 g=step(x0.yzx,x0.xyz);vec3 l=1.-g;vec3 i1=min(g.xyz,l.zxy);vec3 i2=max(g.xyz,l.zxy);vec3 x1=x0-i1+C.xxx;vec3 x2=x0-i2+C.yyy;vec3 x3=x0-D.yyy;i=mod289(i);vec4 p=permute(permute(permute(i.z+vec4(0.,i1.z,i2.z,1.))+i.y+vec4(0.,i1.y,i2.y,1.))+i.x+vec4(0.,i1.x,i2.x,i2.x*0.+1.));float n_=.142857142857;vec3 ns=n_*D.wyz-D.xzx;vec4 j=p-49.*floor(p*ns.z*ns.z);vec4 x_=floor(j*ns.z);vec4 y_=floor(j-7.*x_);vec4 x=x_*ns.x+ns.yyyy;vec4 y=y_*ns.x+ns.yyyy;vec4 h=1.-abs(x)-abs(y);vec4 b0=vec4(x.xy,y.xy);vec4 b1=vec4(x.zw,y.zw);vec4 s0=floor(b0)*2.+1.;vec4 s1=floor(b1)*2.+1.;vec4 sh=-step(h,vec4(0.));vec4 a0=b0.xzyw+s0.xzyw*sh.xxyy;vec4 a1=b1.xzyw+s1.xzyw*sh.zzww;vec3 p0=vec3(a0.xy,h.x);vec3 p1=vec3(a0.zw,h.y);vec3 p2=vec3(a1.xy,h.z);vec3 p3=vec3(a1.zw,h.w);vec4 norm=taylorInvSqrt(vec4(dot(p0,p0),dot(p1,p1),dot(p2,p2),dot(p3,p3)));p0*=norm.x;p1*=norm.y;p2*=norm.z;p3*=norm.w;vec4 m=max(.6-vec4(dot(x0,x0),dot(x1,x1),dot(x2,x2),dot(x3,x3)),0.);m=m*m;return 42.*dot(m*m,vec4(dot(p0,x0),dot(p1,x1),dot(p2,x2),dot(p3,x3)));}`;
const vertexShader=`varying vec2 vUv;void main(){vUv=uv;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`;
const fragmentShader=`uniform float uTime;uniform vec2 uMouse;uniform float uMousePace;uniform float uAspect;uniform float uDarkMix;varying vec2 vUv;${simplexNoise}const float SCALE=.72;const float NOISE_DETAIL=3.;const float DISTORT_SCALE=.55;const float DISTORT_INTENSITY=.50;const float HAIRLINE_PIXELS=1.5;const float CURSOR_SCALE=1.5;const float CURSOR_INTENSITY=.05;const vec2 ANISOTROPY=vec2(1.,1.);void main(){vec2 uv=vUv;uv.x*=uAspect;vec2 mouse=uMouse*.5+.5;mouse.x*=uAspect;float cursor=1.-distance(mouse,uv)*CURSOR_SCALE;cursor*=uMousePace;cursor=clamp(cursor,0.,1.);float noiseDistort=.5+snoise(vec3(uv*DISTORT_SCALE,uTime*.1))*.5;vec2 warpedUv=(uv+cursor*CURSOR_INTENSITY+noiseDistort*DISTORT_INTENSITY)*SCALE*ANISOTROPY;float n=snoise(vec3(warpedUv,uTime));float bands=(n*.5+.5)*NOISE_DETAIL;float contour=fract(bands);float dist=abs(contour-.5);float w=fwidth(bands)*HAIRLINE_PIXELS*.5;float line=1.-smoothstep(0.,w,dist);vec3 bgLight=vec3(.957,.957,.929);vec3 lineLight=vec3(.46,.46,.46);vec3 bgDark=vec3(.024,.047,.102);vec3 lineDark=vec3(1.,1.,1.);vec3 bg=mix(bgLight,bgDark,uDarkMix);vec3 lineCol=mix(lineLight,lineDark,uDarkMix);lineCol+=cursor*.04;float lineAlpha=line*mix(.55,.45,uDarkMix);vec3 color=mix(bg,lineCol,lineAlpha);gl_FragColor=vec4(color,1.);}`;
const isDarkTheme=()=>document.documentElement.dataset.theme==='dark';const motionDisabled=()=>matchMedia('(prefers-reduced-motion: reduce)').matches||document.documentElement.classList.contains('a11y-reduce-motion');const coarsePointer=()=>('ontouchstart'in window)||(matchMedia&&matchMedia('(pointer: coarse)').matches)||navigator.maxTouchPoints>0;
function mountYourBrandBackground(){const host=document.querySelector('.hero-canvas');if(!host)return;host.querySelectorAll('canvas').forEach(n=>n.remove());const scene=new Scene(),camera=new OrthographicCamera(-1,1,1,-1,0,10);camera.position.z=1;const renderer=new WebGLRenderer({antialias:false});renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(Math.min(devicePixelRatio,coarsePointer()?.65:.85));renderer.domElement.dataset.engine='three.js-r183-branddemo-herocanvas';renderer.domElement.setAttribute('aria-hidden','true');host.appendChild(renderer.domElement);const mouse=new Vector2(0,0),darkMix={value:isDarkTheme()?1:0};let darkTarget=darkMix.value;renderer.setClearColor(darkMix.value?461588:16053485,1);const onTheme=()=>{const d=isDarkTheme();darkTarget=d?1:0;renderer.setClearColor(d?461588:16053485,1);if(motionDisabled()){darkMix.value=darkTarget;renderer.render(scene,camera)}};addEventListener('themechange',onTheme);const geometry=new PlaneGeometry(2,2),material=new ShaderMaterial({vertexShader,fragmentShader,uniforms:{uTime:{value:0},uMouse:{value:mouse},uMousePace:{value:0},uAspect:{value:innerWidth/innerHeight},uDarkMix:darkMix}});scene.add(new Mesh(geometry,material));let target={x:0,y:0},smooth={x:0,y:0};const move=e=>{target.x=e.clientX/innerWidth*2-1;target.y=-(e.clientY/innerHeight)*2+1};addEventListener('mousemove',move);const resize=()=>{renderer.setSize(innerWidth,innerHeight);material.uniforms.uAspect.value=innerWidth/innerHeight};addEventListener('resize',resize);let time=0,prev=performance.now(),px=0,py=0,pace=0,raf=0;function frame(now=performance.now()){raf=requestAnimationFrame(frame);if(document.hidden||motionDisabled()||now-prev<50)return;const dt=Math.min(.05,(now-prev)/1000);prev=now;time+=.09*dt;const e=1-Math.exp(-4*dt);smooth.x+=(target.x-smooth.x)*e;smooth.y+=(target.y-smooth.y)*e;mouse.set(smooth.x,smooth.y);const vx=(smooth.x-px)/Math.max(dt,.001),vy=(smooth.y-py)/Math.max(dt,.001),tp=Math.min(1,Math.hypot(vx,vy)*.4),pe=1-Math.exp(-8*dt);pace+=(tp-pace)*pe;px=smooth.x;py=smooth.y;darkMix.value+=(darkTarget-darkMix.value)*(1-Math.exp(-7*dt));material.uniforms.uTime.value=time;material.uniforms.uMousePace.value=pace;renderer.render(scene,camera)}renderer.render(scene,camera);frame();window.__branddemoBackground={renderer,scene,camera,material,destroy(){cancelAnimationFrame(raf);removeEventListener('mousemove',move);removeEventListener('resize',resize);removeEventListener('themechange',onTheme);geometry.dispose();material.dispose();renderer.dispose();renderer.domElement.remove()}}}

try { mountYourBrandBackground(); } catch (error) { console.warn("Background animation unavailable", error); }
