const {reply,guard,text,id,hash,save}=require('../lib/demo-store');
module.exports=async(req,res)=>{try{if(!guard(req,res))return;const b=req.body||{};if(b.website)return reply(res,400,{error:'Invalid request'});const business=text(b.business),name=text(b.name),email=text(b.email,180).toLowerCase(),needs=text(b.needs,2000);if(!business||!name||!/^\S+@\S+\.\S+$/.test(email)||b.consent!=='on')return reply(res,400,{error:'Complete the required details and consent'});const at=new Date().toISOString(),key=hash(email+'|'+business+'|'+at.slice(0,13));await save(`inquiries/${at.slice(0,10)}/${key}.json`,{business,name,email,needs,client:id(b.client)||'general',brand:text(b.brand),color:/^#[0-9a-f]{6}$/i.test(b.color)?b.color:null,consent:true,at});return reply(res,201,{saved:true})}catch{return reply(res,503,{error:'Your request was not saved. Please try again.'})}};
