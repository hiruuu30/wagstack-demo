const {reply,configured,isAdmin,records}=require('../lib/demo-store');
module.exports=async(req,res)=>{try{if(req.method!=='GET')return reply(res,405,{error:'Method not allowed'});if(!isAdmin(req))return reply(res,401,{error:'A valid report access key is required'});if(!configured())return reply(res,503,{error:'Private storage is not connected'});const kind=req.query.kind==='inquiries'?'inquiries':'events',date=String(req.query.date||new Date().toISOString().slice(0,10));if(!/^\d{4}-\d{2}-\d{2}$/.test(date))return reply(res,400,{error:'Invalid date'});return reply(res,200,await records(`${kind}/${date}/`,req.query.cursor||undefined))}catch{return reply(res,503,{error:'Report temporarily unavailable'})}};
